name: TikTok Analytics CI

on:
  push:
    branches: [main]
    paths:
      - "scripts/tiktok_analytics.py"
      - "scripts/tiktok_report.py"
      - "analytics/manual/**"
      - ".github/workflows/tiktok-analytics*.yml"
      - "requirements.txt"
      - "pyproject.toml"
  pull_request:
    paths:
      - "scripts/tiktok_analytics.py"
      - "scripts/tiktok_report.py"
      - "analytics/manual/**"
      - ".github/workflows/tiktok-analytics*.yml"
      - "requirements.txt"
      - "pyproject.toml"

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install minimal dependencies
        run: python -m pip install --disable-pip-version-check "cryptography>=43,<47" "python-dotenv>=1,<2"
      - name: Compile analytics tools
        run: python -m py_compile scripts/tiktok_analytics.py scripts/tiktok_report.py
      - name: Smoke test collector CLI
        run: |
          python scripts/tiktok_analytics.py --help
          python scripts/tiktok_analytics.py keygen | python -c "import sys; from cryptography.fernet import Fernet; Fernet(sys.stdin.read().strip().encode()); print('fernet key ok')"
      - name: Generate report from manual baseline
        run: |
          python scripts/tiktok_report.py
          test -s analytics/report.md
          grep -q "TikTok コンディションレポート" analytics/report.md
          grep -q "テセウスの船" analytics/report.md
